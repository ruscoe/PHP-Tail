A script to emulate the UNIX tail function by displaying the last X number of lines in a file.

Useful for large files, such as logs, when you want to process lines in PHP or write lines to a database.
